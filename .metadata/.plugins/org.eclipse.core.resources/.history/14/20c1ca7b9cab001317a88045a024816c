package hw4;
import java.util.*;

public class Process {
	int memSize;
	int duration;
	int name;
	public Process()
	{
		Random rGen = new Random();
		memSize = 2 ^ (rGen.nextInt(4) + 2);
		duration = rGen.nextInt(5) + 1;
	}

}
